\documentclass[tikz,border=2pt]{standalone}

\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,calc,backgrounds}

\definecolor{slate900}{HTML}{0F172A}
\definecolor{slate700}{HTML}{334155}
\definecolor{slate500}{HTML}{64748B}
\definecolor{slate300}{HTML}{CBD5E1}
\definecolor{slate100}{HTML}{F1F5F9}
\definecolor{blue700}{HTML}{2563EB}
\definecolor{blue100}{HTML}{DBEAFE}
\definecolor{teal700}{HTML}{0F766E}
\definecolor{teal100}{HTML}{CCFBF1}
\definecolor{amber700}{HTML}{D97706}
\definecolor{amber100}{HTML}{FEF3C7}

\begin{document}
\begin{tikzpicture}[
  font=\sffamily,
  stage/.style={
    draw=slate700,
    rounded corners=3.5mm,
    fill=white,
    line width=0.95pt,
    minimum height=22mm,
    minimum width=30mm,
    align=center,
    text=slate900,
    inner sep=3.2mm
  },
  statebox/.style={
    draw=teal700,
    rounded corners=3.5mm,
    fill=teal100,
    line width=0.95pt,
    minimum height=17mm,
    minimum width=34mm,
    align=center,
    inner sep=3mm,
    text=slate900
  },
  memorybox/.style={
    draw=blue700,
    rounded corners=3.5mm,
    fill=blue100,
    line width=1pt,
    minimum height=19mm,
    minimum width=38mm,
    align=center,
    inner sep=3.2mm,
    text=slate900
  },
  externalbox/.style={
    draw=slate700,
    rounded corners=3.5mm,
    fill=slate100,
    line width=0.95pt,
    minimum height=18mm,
    minimum width=33mm,
    align=center,
    inner sep=3mm,
    text=slate900
  },
  stageflow/.style={draw=slate900, line width=1.35pt, -{Stealth[length=2.2mm,width=1.6mm]}},
  memflow/.style={draw=blue700, line width=1.12pt, -{Stealth[length=2mm,width=1.45mm]}},
  regflow/.style={draw=teal700, line width=1.08pt, -{Stealth[length=2mm,width=1.45mm]}},
  controlflow/.style={draw=amber700, line width=1.22pt, -{Stealth[length=2.2mm,width=1.6mm]}},
  annotation/.style={font=\scriptsize, text=slate500, align=center, fill=white, inner sep=1pt},
  sectiontitle/.style={font=\bfseries\small, text=slate700, fill=slate100, inner sep=1pt},
  sectionrule/.style={draw=slate300, line width=0.7pt},
  boundary/.style={
    draw=slate700,
    rounded corners=5mm,
    fill=slate100,
    line width=1.2pt,
    inner sep=7mm
  },
  overallboundary/.style={
    draw=slate300,
    rounded corners=6mm,
    line width=0.9pt,
    fill=none,
    inner sep=7mm
  },
  badge/.style={
    draw=amber700,
    rounded corners=3mm,
    fill=amber100,
    line width=0.95pt,
    inner xsep=4.5mm,
    inner ysep=2mm,
    text=slate900,
    font=\bfseries\scriptsize,
    align=center
  }
]

% Row B — instruction lifecycle
\node[stage, minimum width=31mm] (ifu) {
  {\bfseries IFU}\\[-1mm]
  {\scriptsize 5-state Fetch FSM}\\[-0.6mm]
  {\scriptsize PC $\rightarrow$ instruction}
};
\node[stage, minimum width=33mm, right=14mm of ifu] (idu) {
  {\bfseries IDU}\\[-1mm]
  {\scriptsize 3-state Decode FSM}\\[-0.6mm]
  {\scriptsize ControlUnit \textperiodcentered{} ImmGen}
};
\node[stage, minimum width=42mm, right=14mm of idu] (exu) {
  {\bfseries EXU}\\[-1mm]
  {\scriptsize 3-state Execute FSM}\\[-0.6mm]
  {\scriptsize ALU \textperiodcentered{} Comparator \textperiodcentered{} PC Target}
};
\node[stage, minimum width=36mm, right=14mm of exu] (memu) {
  {\bfseries MEMU}\\[-1mm]
  {\scriptsize 7-state Memory FSM}\\[-0.6mm]
  {\scriptsize align / sign extension}
};
\node[stage, minimum width=35mm, right=14mm of memu] (wbu) {
  {\bfseries WBU}\\[-1mm]
  {\scriptsize 3-state Writeback FSM}\\[-0.6mm]
  {\scriptsize commit + PC update}
};

% Row A — architectural state
\node[statebox, above=25mm of ifu, minimum width=35mm] (pcstate) {
  {\bfseries PC register}\\[-1mm]
  {\scriptsize \texttt{pc\_r} + \texttt{executing}}
};
\node[statebox, above=25mm of idu, minimum width=35mm] (gpr) {
  {\bfseries GPR File}\\[-1mm]
  {\scriptsize 2R / 1W}
};
\node[statebox, above=25mm of exu, minimum width=40mm] (csr) {
  {\bfseries CSR File}\\[-1mm]
  {\scriptsize 3R / 2W}\\[-0.6mm]
  {\scriptsize mstatus \textperiodcentered{} mtvec \textperiodcentered{} mepc}\\[-0.6mm]
  {\scriptsize mcause \textperiodcentered{} mtval}
};

% Row C / D — memory subsystem
\node[memorybox, below=20mm of ifu, minimum width=43mm] (icache) {
  {\bfseries Instruction Cache}\\[-1mm]
  {\scriptsize blocking direct-mapped I\$}\\[-0.6mm]
  {\scriptsize 8 lines $\times$ 4 B = 32 B}\\[-0.6mm]
  {\scriptsize tag \textperiodcentered{} valid \textperiodcentered{} data}
};
\node[memorybox, below=20mm of memu, xshift=28mm, minimum width=60mm, minimum height=24mm] (lsu) {
  {\bfseries LSU}\\[-1mm]
  {\scriptsize IFetch / MEM arbiter}\\[-0.6mm]
  {\scriptsize align / split \textperiodcentered{} AXI FSM}
};
\node[memorybox, below=18mm of lsu, xshift=-18mm, minimum width=33mm] (clint) {
  {\bfseries CLINT}\\[-1mm]
  {\scriptsize ACLINT MTIME}
};
\node[externalbox, right=18mm of lsu, minimum width=32mm] (axi) {
  {\bfseries AXI4 Master}\\[-1mm]
  {\scriptsize AR / R / AW / W / B}
};
\node[externalbox, right=18mm of axi, minimum width=40mm, minimum height=24mm] (soc) {
  {\bfseries ysyxSoC XBar}\\[-1mm]
  {\scriptsize Flash / PSRAM / SDRAM / MMIO}
};

% Core boundary first so titles/lanes can reference it
\begin{scope}[on background layer]
  \node[boundary, fit=(pcstate)(gpr)(csr)(ifu)(idu)(exu)(memu)(wbu)(icache)(lsu)(clint)(axi)] (core-boundary) {};
  \node[overallboundary, fit=(core-boundary)(soc)] (diagram-boundary) {};
\end{scope}

% Explicit section titles instead of fit labels
\node[sectiontitle, anchor=north west] (core-title) at ($(core-boundary.north west)+(8mm,-5mm)$) {NPC Core / \texttt{ysyx\_25070190}};
\node[badge, anchor=north east] (mode-badge) at ($(diagram-boundary.north east)+(-8mm,-4mm)$) {single instruction in flight\\multi-cycle FSM};

\node[sectiontitle, anchor=west] (state-title) at ($(core-boundary.west)+(12mm,28mm)$) {Architectural state / control};
\draw[sectionrule] ($(state-title.east)+(3mm,0)$) -- ($(core-boundary.east)+(-10mm,28mm)$);

\node[sectiontitle, anchor=west] (life-title) at ($(core-boundary.west)+(12mm,3mm)$) {Instruction lifecycle};
\draw[sectionrule] ($(life-title.east)+(3mm,0)$) -- ($(core-boundary.east)+(-10mm,3mm)$);

\node[sectiontitle, anchor=west] (mem-title) at ($(core-boundary.west)+(12mm,-31mm)$) {Memory subsystem};
\draw[sectionrule] ($(mem-title.east)+(3mm,0)$) -- ($(core-boundary.east)+(-10mm,-31mm)$);

% Logical ports and routing lanes
\coordinate (idu-gpr-in) at ($(idu.north)+(-8mm,0)$);
\coordinate (idu-csr-in) at ($(idu.north)+(8mm,0)$);

\coordinate (memu-lsu-port) at ($(memu.south)+(-7mm,0)$);
\coordinate (memu-clint-port) at ($(memu.south)+(7mm,0)$);
\coordinate (lsu-data-in) at ($(lsu.north)+(-12mm,0)$);
\coordinate (clint-data-in) at ($(clint.north)+(0,0)$);
\coordinate (csr-read-lane) at ($(csr.south)+(0,-8mm)$);

\coordinate (wbu-fanout) at ($(wbu.north)+(0,3mm)$);
\coordinate (gpr-write-lane-start) at ($(wbu-fanout)+(0,5mm)$);
\coordinate (csr-write-lane-start) at ($(wbu-fanout)+(0,11mm)$);
\coordinate (pc-feedback-lane-start) at ($(wbu-fanout)+(0,17mm)$);

\coordinate (gpr-write-lane-end) at ($(gpr.north |- gpr-write-lane-start)$);
\coordinate (csr-write-lane-end) at ($(csr.north |- csr-write-lane-start)$);
\coordinate (pc-feedback-lane-end) at ($(pcstate.east |- pc-feedback-lane-start)$);

\coordinate (memu-lsu-lane) at ($(memu-lsu-port)+(0,-7mm)$);
\coordinate (memu-clint-lane) at ($(memu-clint-port)+(0,-12mm)$);

% Main stage row
\draw[stageflow] (ifu.east) -- node[midway, above, annotation] {\texttt{IF\_ID}} (idu.west);
\draw[stageflow] (idu.east) -- node[midway, above, annotation] {\texttt{ID\_EX}} (exu.west);
\draw[stageflow] (exu.east) -- node[midway, above, annotation] {\texttt{EX\_MEM}} (memu.west);
\draw[stageflow] (memu.east) -- node[midway, above, annotation] {\texttt{MEM\_WB}} (wbu.west);
\node[annotation, above=6mm of exu] (bundle-note) {Decoupled valid / ready};

% Architectural-state reads
\draw[controlflow] (pcstate.south) -- node[midway, left, annotation] {executionInfo.pc} (ifu.north);
\draw[regflow] (gpr.south) -- node[midway, left, annotation] {rs1 / rs2\\2 read ports} (idu-gpr-in);
\draw[regflow] (csr.south) -- (csr-read-lane);
\draw[regflow] (csr-read-lane) -- node[midway, above, annotation] {CSR / mepc / mtvec\\3R} ($(idu-csr-in |- csr-read-lane)$);
\draw[regflow] ($(idu-csr-in |- csr-read-lane)$) -- (idu-csr-in);

% Feedback lanes — three explicit corridors
\draw[regflow] (wbu.north) -- (wbu-fanout) -- (gpr-write-lane-start) -- node[midway, above, annotation] {rd writeback} (gpr-write-lane-end) -- (gpr.north);
\draw[regflow] (wbu.north) -- (wbu-fanout) -- (csr-write-lane-start) -- node[midway, above, annotation] {CSR write / ecall state} (csr-write-lane-end) -- (csr.north);
\draw[controlflow] (wbu.north) -- (wbu-fanout) -- (pc-feedback-lane-start) -- node[midway, above, annotation] {commit: done + pcTargetOut} (pc-feedback-lane-end) -- (pcstate.east);

% Memory subsystem
\draw[memflow] (ifu.south) -- node[midway, right, annotation] {cpu req / resp} (icache.north);
\draw[memflow] (icache.east) -- node[midway, above, annotation] {lower req / resp} (lsu.west);

\draw[memflow] (memu-lsu-port) -- (memu-lsu-lane);
\draw[memflow] (memu-lsu-lane) -- node[midway, above, annotation] {mem req / resp} ($(lsu-data-in |- memu-lsu-lane)$);
\draw[memflow] ($(lsu-data-in |- memu-lsu-lane)$) -- (lsu-data-in);

\draw[memflow] (memu-clint-port) -- (memu-clint-lane);
\draw[memflow] (memu-clint-lane) -- node[midway, above, annotation] {AXI4-Lite / MTIME} ($(clint-data-in |- memu-clint-lane)$);
\draw[memflow] ($(clint-data-in |- memu-clint-lane)$) -- (clint-data-in);

\draw[memflow] (lsu.east) -- node[midway, above, annotation] {\texttt{io.memBus}} (axi.west);
\draw[stageflow] (axi.east) -- node[midway, above, annotation] {Top.master} (soc.west);

% Footer note
\node[annotation, below=2mm of diagram-boundary.south] {Functional RTL topology only --- DPI / perf observability omitted from the main datapath view};

\end{tikzpicture}
\end{document}
